module.exports = function (app) {

	// var movie = require('./services/movie.ctrl');
	/* route file include*/
	// var user = require('./services/user.ctrl')(app, passport)
	// var user = require('./services/user.ctrl');
	//  require('../config/passport')( passport)
	// require('./services/temp.route')(app,passport);
	// app.get('/', function(req, res){
	// 	res.render('index', {user:req.user})
	// })
	// app.use('/user',user)
	// app.use('/movie', movie);
	// // home page
	// app.use(function(req, res, next) {
	// 	res.locals.user = req.user;
	// 	console.log(res.locals.user);
	// 	next()
	// })

}